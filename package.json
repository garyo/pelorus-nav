{
  "name": "pelorus-nav",
  "private": true,
  "version": "0.1.0",
  "description": "Pelorus Nav — open-source web-based marine chartplotter",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.worker.json --noEmit",
    "check": "bun run typecheck && bun run lint && bun run test",
    "sprites": "spreet tools/sprites/svg public/sprites/nautical && spreet --retina tools/sprites/svg public/sprites/nautical@2x && printf '\\n' >> public/sprites/nautical.json && printf '\\n' >> public/sprites/nautical@2x.json && bun tools/sprites/build-s52-sprites.ts",
    "tiles": "tools/build-tiles.sh boston-test",
    "tiles:newengland": "tools/build-tiles.sh new-england",
    "tiles:usvi": "tools/build-tiles.sh usvi",
    "tiles:all": "tools/build-tiles.sh all",
    "tiles:download": "cd tools/s57-pipeline && uv run python -m s57_pipeline download --region boston-test",
    "tiles:download:newengland": "cd tools/s57-pipeline && uv run python -m s57_pipeline download --region new-england",
    "tiles:download:usvi": "cd tools/s57-pipeline && uv run python -m s57_pipeline download --region usvi",
    "tiles:upload": "tools/upload-tiles.sh",
    "tiles:upload:force": "tools/upload-tiles.sh --force",
    "deploy": "bun run build && wrangler deploy"
  },
  "license": "MIT",
  "dependencies": {
    "maplibre-gl": "^5.19.0",
    "pmtiles": "^4.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@cloudflare/workers-types": "^4.20260305.1",
    "@playwright/test": "^1.58.2",
    "@vitest/coverage-v8": "^4.0.18",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vite-plugin-pwa": "^1.2.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.70.0"
  }
}
