<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- GPS permissions. Screen-off tracking is covered by the location-type
         foreground service; ACCESS_BACKGROUND_LOCATION is deliberately NOT
         declared (never requested, and it invites Play policy rejection). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Bluetooth LE permissions (BLE GPS pod via @capacitor-community/bluetooth-le) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Legacy (Android < 12) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Notification permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Crew-overboard alarm vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Auto Backup stays ON (routes/settings live in WebView storage) but
         backup_rules.xml / data_extraction_rules.xml exclude the service-
         worker dirs: a restored SW + cache boots an ancient build on fresh
         installs (cached build predates the startup SW-unregister). -->
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- "Open with" for GPX files. Providers that type GPX correctly.
                 Kept separate from the filter below because <data> attributes
                 cross-product within a single filter.

                 GPX has no registered IANA type, so file managers invent one:
                 Solid Explorer and others send application/gpx or text/gpx
                 rather than the +xml form. All of these name GPX explicitly,
                 so claiming them costs nothing. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/gpx" />
                <data android:mimeType="application/vnd.google-earth.gpx+xml" />
                <data android:mimeType="application/vnd.google-earth.gpx" />
                <data android:mimeType="text/gpx+xml" />
                <data android:mimeType="text/gpx" />
                <data android:mimeType="text/xml+gpx" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
            </intent-filter>

            <!-- Types that aren't GPX-specific, but are what the file actually
                 arrives as. Cloud providers report octet-stream and their
                 content:// URIs carry no filename, so MIME is all there is to
                 match on; Solid Explorer reports text/plain (GPX Viewer claims
                 it for the same reason). The cost is being offered for
                 unrelated blobs and text files — gpx-open sniffs for "<gpx"
                 and declines those with a plain message. Without these,
                 Pelorus is absent from Drive and half the file managers. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Legacy file:// senders (rare since Android 7). Here pathPattern
                 works, so match by extension; the repeated patterns are the
                 standard workaround for ".*" not backtracking across dots. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.gpx" />
                <data android:pathPattern=".*\\..*\\.gpx" />
                <data android:pathPattern=".*\\..*\\..*\\.gpx" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.gpx" />
                <data android:pathPattern=".*\\.GPX" />
                <data android:pathPattern=".*\\..*\\.GPX" />
                <data android:pathPattern=".*\\..*\\..*\\.GPX" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.GPX" />
            </intent-filter>

        </activity>

        <!-- Background GPS track recording service -->
        <service
            android:name="nav.pelorus.plugins.backgroundgps.BackgroundTrackService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

</manifest>
